- name: Install nginx and run
  hosts: frontend
  tasks:
  - name: Install nginx
    ansible.bultin.package:
      name: nginx
      state: installed

  - name: Start nginx
    ansible.bultin.service:
      name: nginx
      state: started