- name: Install nginx
  hosts: dynamic_frontend
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.dnf:
      name: nginx
      state: installed
  
  - name: Run and enable nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
  
  